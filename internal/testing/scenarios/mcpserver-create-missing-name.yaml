name: "mcpserver-create-missing-name"
category: "behavioral"
concept: "mcpserver"
description: "Tests creating an MCPServer with a missing name."
tags: ["mcpserver", "create", "error-handling", "core-api"]
timeout: "1m"

steps:
- id: "create-missing-name"
  description: "Attempt to create a server without a name"
  tool: "core_mcpserver_create"
  args:
    yaml: |
      type: localCommand
      command: ["echo", "hi"]
  expected:
    success: false
    error_contains: ["name is required"] 