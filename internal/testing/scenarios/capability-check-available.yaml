name: "capability-check-available"
category: "behavioral"
concept: "capability"
tags: ["capability", "available", "core-api"]
timeout: "1m"

pre_configuration:
  capabilities:
    - name: "available-test-capability"
      config:
        name: "available-test-capability"
        # type: "test" # removed - no longer supported

steps:
- id: "check-is-available"
  tool: "core_mcpserver_available"
  args:
    name: "available-test-capability"
  expected:
    success: true
    json_path:
      available: true 