name: "capability-get"
category: "behavioral"
concept: "capability"
description: "Tests getting a specific capability definition."
tags: ["capability", "get", "core-api"]
timeout: "1m"

pre_configuration:
  capabilities:
    - name: "get-test-capability"
      config:
        name: "get-test-capability"
        type: "test"
        description: "A capability for testing get"

steps:
- id: "get-specific-capability"
  description: "Get the pre-configured capability by name"
  tool: "core_capability_get"
  args:
    name: "get-test-capability"
  expected:
    success: true
    json_path:
      name: "get-test-capability"
      description: "A capability for testing get" 