name: "workflow-create-duplicate-step-ids"
category: "behavioral"
concept: "workflow"
description: "Tests creating a Workflow with duplicate step IDs."
tags: ["workflow", "create", "validation", "error-handling", "core-api"]
timeout: "1m"

steps:
- id: "create-duplicate-step-ids"
  description: "Attempt to create a Workflow with duplicate step IDs"
  tool: "core_workflow_create"
  args:
    name: "duplicate-steps-workflow"
    description: "Workflow with duplicate step IDs"
    steps:
      - id: "duplicate-step"
        tool: "core_capability_list"
      - id: "duplicate-step"
        tool: "core_mcpserver_list"
  expected:
    success: false
    error_contains: ["duplicate", "step id"] 