name: "workflow-spec-get-schema"
category: "behavioral"
concept: "workflow"
tags: ["workflow", "spec", "core-api"]
timeout: "1m"

steps:
- id: "get-schema-spec"
  tool: "core_workflow_get"
  args:
  expected:
    success: true
    contains: ["schema"]
    not_contains: ["template"] 