name: "service-get"
category: "behavioral"
concept: "service"
tags: ["service", "get", "core-api"]
timeout: "1m"

pre_configuration:
  service_classes:
  - name: "sc-for-get"
    config:
      name: "sc-for-get"
      serviceConfig:
        lifecycleTools:
          start: { tool: "core_config_get" }
          stop: { tool: "core_config_get" }
steps:
- id: "create-service-for-get"
  tool: "core_service_create"
  args: { serviceClassName: "sc-for-get", name: "get-test-service" }
  expected: { success: true }
  cleanup:
  - tool: "core_service_delete"
    args: { name: "get-test-service" }
- id: "get-the-service"
  tool: "core_service_get"
  args:
    name: "get-test-service"
  expected:
    success: true
    json_path:
      name: "get-test-service"
      state: "RUNNING" 