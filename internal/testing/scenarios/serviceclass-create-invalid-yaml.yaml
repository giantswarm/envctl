name: "serviceclass-create-invalid-yaml"
category: "behavioral"
concept: "serviceclass"
description: "Tests creating a ServiceClass with invalid YAML syntax."
tags: ["serviceclass", "create", "error-handling", "core-api"]
timeout: "1m"

steps:
- id: "create-with-invalid-yaml"
  description: "Attempt to create a ServiceClass using a syntactically incorrect YAML string"
  tool: "core_serviceclass_create"
  args:
    yaml: "name: broken: yaml:"
  expected:
    success: false
    error_contains: ["invalid YAML"] 