name: "workflow-delete-non-existent"
category: "behavioral"
concept: "workflow"
description: "Tests deleting a workflow that does not exist."
tags: ["workflow", "delete", "error-handling", "not-found", "core-api"]
timeout: "1m"

steps:
- id: "delete-non-existent"
  description: "Attempt to delete a workflow by a name that is not registered"
  tool: "core_workflow_delete"
  args:
    name: "non-existent-workflow"
  expected:
    success: false
    error_contains: ["not found"] 