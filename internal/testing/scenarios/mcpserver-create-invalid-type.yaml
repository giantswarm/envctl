name: "mcpserver-create-invalid-type"
category: "behavioral"
concept: "mcpserver"
description: "Tests creating an MCPServer with an invalid type."
tags: ["mcpserver", "create", "validation", "error-handling", "core-api"]
timeout: "1m"

steps:
- id: "create-invalid-type"
  description: "Attempt to create an MCPServer with invalid type"
  tool: "core_mcpserver_create"
  args:
    name: "invalid-type-server"
    type: "invalid-type"
    enabled: true
    command:
      - "echo"
      - "test"
  expected:
    success: false
    error_contains: ["invalid type", "unknown type"] 