name: "mcpserver-register"
category: "behavioral"
concept: "mcpserver"
description: "Tests registering an MCP server definition."
tags: ["mcpserver", "register", "core-api"]
timeout: "1m"

steps:
- id: "register-mcpserver"
  description: "Register an MCP server and verify it's available"
  tool: "core_mcpserver_register"
  args:
    name: "register-test-server"
    type: "localCommand"
    enabled: true
    icon: "🔧"
    category: "testing"
    command: ["echo", "register-test"]
    env:
      TEST_MODE: "register"
      LOG_LEVEL: "debug"
  expected:
    success: true
  cleanup:
    - id: "unregister-mcpserver"
      tool: "core_mcpserver_unregister"
      args:
        name: "register-test-server" 