name: "capability-check-unavailable"
category: "behavioral"
concept: "capability"
tags: ["capability", "available", "not-found", "core-api"]
timeout: "1m"

steps:
- id: "check-is-unavailable"
  tool: "core_mcpserver_available"
  args:
    name: "i-do-not-exist"
  expected:
    success: true
    json_path:
      available: false 