name: "mcpserver-get"
category: "behavioral"
concept: "mcpserver"
tags: ["mcpserver", "get", "core-api"]
timeout: "1m"

pre_configuration:
  mcp_servers:
  - name: "get-test-server"
    config:
      name: "get-test-server"
steps:
- id: "get-specific-server"
  tool: "core_mcpserver_get"
  args:
    name: "get-test-server"
  expected:
    success: true
    json_path:
      name: "get-test-server"
