name: "mcpserver-get"
category: "behavioral"
concept: "mcpserver"
description: "Tests getting a specific MCPServer."
tags: ["mcpserver", "get", "core-api"]
timeout: "1m"

pre_configuration:
  mcp_servers:
  - name: "get-test-server"
    config:
      name: "get-test-server"
      type: "localCommand"
      command: ["echo", "get test"]
steps:
- id: "get-specific-server"
  description: "Get a pre-configured server"
  tool: "core_mcpserver_get"
  args:
    name: "get-test-server"
  expected:
    success: true
    json_path:
      name: "get-test-server"
      type: "localCommand" 