name: "capability-create-no-operations"
category: "behavioral"
concept: "capability"
description: "Tests creating a capability without operations (should fail)."
tags: ["capability", "create", "validation", "error-handling", "core-api"]
timeout: "1m"

steps:
- id: "create-no-operations-capability"
  description: "Attempt to create a capability definition without operations"
  tool: "core_capability_create"
  args:
    name: "no-operations-capability"
    type: "test"
    description: "A capability with no operations"
    operations: {}
  expected:
    success: false
    error_contains: ["must have at least one operation"] 