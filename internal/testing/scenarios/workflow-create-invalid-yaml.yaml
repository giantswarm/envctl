name: "workflow-create-invalid-yaml"
category: "behavioral"
concept: "workflow"
description: "Tests creating a Workflow with invalid configuration."
tags: ["workflow", "create", "validation", "error-handling", "core-api"]
timeout: "1m"

steps:
- id: "create-invalid-workflow"
  description: "Attempt to create a Workflow with invalid configuration"
  tool: "core_workflow_create"
  args:
    name: "invalid-workflow"
    description: "Invalid workflow test"
    steps:
      - id: ""  # Invalid empty step id
        description: "Empty step"
        tool: "core_capability_list"
  expected:
    success: false
    error_contains: ["invalid", "step id", "required"] 