name: "workflow-create-invalid-yaml"
category: "behavioral"
concept: "workflow"
description: "Tests creating a workflow with invalid YAML syntax."
tags: ["workflow", "create", "error-handling", "core-api"]
timeout: "1m"

steps:
- id: "create-with-invalid-yaml"
  description: "Attempt to create a workflow using an invalid YAML string"
  tool: "core_workflow_create"
  args:
    yaml_definition: "name: yaml is broken"
  expected:
    success: false
    error_contains: ["invalid YAML"] 