name: "serviceclass-refresh"
category: "behavioral"
concept: "serviceclass"
description: "Tests the 'refresh' command for ServiceClasses."
tags: ["serviceclass", "refresh", "core-api"]
timeout: "1m"

steps:
- id: "refresh-all-scs"
  description: "Execute the 'refresh' command to update availability"
  tool: "core_serviceclass_refresh"
  expected:
    success: true 