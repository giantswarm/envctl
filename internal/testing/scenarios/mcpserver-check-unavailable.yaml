name: "mcpserver-check-unavailable"
description: "Test checking availability of non-existent MCP servers"
category: "behavioral"
concept: "mcpserver"
tags: ["mcpserver", "unavailable", "core-api"]
timeout: "1m"

steps:
- id: "check-unavailable"
  description: "Check availability of a non-existent MCP server"
  tool: "core_mcpserver_available"
  args:
    name: "non-existent-server"
  expected:
    success: true
    json_path:
      available: false 